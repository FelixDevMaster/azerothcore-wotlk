# lua_scripts

Eluna/ALE loads every `.lua` file in this folder (the same directory as `AIO.lua`).

`RBG_Server.lua` and `RBG_Client.lua` are the Rated Battleground AIO scripts.
Place Rochet2 `AIO.lua` (from `AIO_Server`) in this folder before starting worldserver.
